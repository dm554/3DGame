#include "Camera.h"

void camera_update(){
	camera_mouse_input();
}

void camera_mouse_input(){
	/*SDL_Event *pEvent;
	if (SDL_PollEvent(pEvent) != 0 && SDL_PollEvent(pEvent) == SDL_MOUSEMOTION){
		//gf3d_vgraphics_rotate_camera(pEvent->motion.x);
	}*/
}